import React from 'react';
import { Route, Switch } from 'react-router-dom';

const Routes = props => {
  return (
    <section>
      <Switch>
      
      </Switch>
    </section>
  );
};

export default Routes;
